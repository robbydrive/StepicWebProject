server {
  listen 80 default_server;
  access_log /home/box/web/nginx.access_log info; 
  location ^~ /uploads/ {
    root /home/box/web;
  }
  location ~* ^.+\.\w+$ {
    root /home/box/web/public;
  }
  location / {
    return 404;
  }
}
